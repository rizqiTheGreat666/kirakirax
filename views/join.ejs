<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KiraX | <%= title %></title>
    <link rel="stylesheet" href="/css/style.css"> 
    <style>
        .form-container { display: flex; justify-content: center; max-width: 500px; margin: 60px auto; padding: 20px; }
        .auth-form { flex: 1; padding: 30px; border: 1px solid #ddd; border-radius: 6px; background: #fff; box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
        .auth-form h3 { text-align: center; margin-bottom: 25px; color: #333; }
        .auth-form input { width: 100%; padding: 12px; margin-bottom: 15px; border: 1px solid #ccc; border-radius: 4px; box-sizing: border-box; font-size: 1em; }
        .auth-form input:focus { outline: none; border-color: #007bff; box-shadow: 0 0 4px rgba(0,123,255,0.3); }
        .auth-form button { background-color: #007bff; color: white; padding: 12px 15px; border: none; border-radius: 4px; cursor: pointer; width: 100%; font-weight: 600; font-size: 1.1em; transition: background-color 0.3s; }
        .auth-form button:hover { background-color: #0056b3; }
        .message { padding: 10px; margin-bottom: 20px; border-radius: 4px; color: #fff; background-color: #28a745; text-align: center; }
        .message.error { background-color: #dc3545; }
        .auth-footer { margin-top: 15px; text-align: center; font-size: 0.9em; color: #666; }
        .auth-footer a { color: #007bff; text-decoration: none; font-weight: 600; }
        .auth-footer a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <%- include('partials/header.ejs') %>

    <main class="content-container">
        <h2><%= title %></h2>

        <% if (message) { %>
            <div class="message <%= message.includes('gagal') || message.includes('error') || message.includes('not found') || message.includes('Incorrect') || message.includes('salah') ? 'error' : '' %>">
                <%= message %>
            </div>
        <% } %>

        <div class="form-container">
            <div class="auth-form">
                <h3><%= lang === 'id' ? 'Masuk ke Akun' : 'Login to Account' %></h3>
                <form action="/login" method="POST">
                    <input type="hidden" name="lang" value="<%= lang %>">
                    <input type="text" name="username" placeholder="Username" required>
                    <input type="password" name="password" placeholder="<%= lang === 'id' ? 'Kata Sandi' : 'Password' %>" required>
                    <button type="submit">
                        <%= lang === 'id' ? 'Masuk' : 'Login' %>
                    </button>
                </form>
                <div class="auth-footer">
                    <%= lang === 'id' ? 'Belum memiliki akun?' : 'Don\'t have an account?' %>
                    <a href="/<%= lang === 'id' ? 'id/daftar' : 'en/register' %>">
                        <%= lang === 'id' ? 'Daftar di sini' : 'Register here' %>
                    </a>
                </div>
            </div>
        </div>
    </main>
    
    <%- include('partials/footer.ejs') %>
</body>
</html>